# Production Environment Variables Example
# Copy this file to .env on your VPS and fill in the actual values

# Database
# PostgreSQL connection string
# Format: postgresql://username:password@localhost:5432/database_name?schema=public
DATABASE_URL="postgresql://kissblow_user:your_password_here@localhost:5432/kissblow?schema=public"

# Redis
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD="your_redis_password_here"

# App
NODE_ENV="production"
NEXT_PUBLIC_APP_URL="https://kissblow.me"

# ATLOS Payment Gateway
ATLOS_MERCHANT_ID="OAK1D092DB"
ATLOS_API_SECRET="4VWilRiqpcJugiHmAZw22hNtTrPyFpCR"

# SMTP Email Configuration (Maileroo)
SMTP_HOST="smtp.maileroo.com"
SMTP_PORT="587"
SMTP_EMAIL="info@kissblow.me"
SMTP_USER="info@kissblow.me"
SMTP_PASSWORD="your_smtp_password_here"
SMTP_FROM="info@kissblow.me"
SMTP_SECURE="false"

# Contact Email (where contact/DMCA form submissions will be sent)
CONTACT_EMAIL="info@kissblow.me"

# Cron Job Secret (for securing the subscription check endpoint)
# Generate a random string: openssl rand -hex 32
CRON_SECRET="generate_random_string_here"

# CSP Report URI (optional, for monitoring CSP violations)
CSP_REPORT_URI=""


