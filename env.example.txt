# Database
# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:DebMiraYas9%21@localhost:5432/kissblow?schema=public"

# Redis
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# App
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# For production, set to: https://kissblow.me

# ATLOS Payment Gateway
ATLOS_MERCHANT_ID="OAK1D092DB"
ATLOS_API_SECRET="4VWilRiqpcJugiHmAZw22hNtTrPyFpCR"

# SMTP Email Configuration (Maileroo)
SMTP_HOST="smtp.maileroo.com"
SMTP_PORT="587"
SMTP_EMAIL="info@kissblow.me"
SMTP_USER="info@kissblow.me"
SMTP_PASSWORD="your_smtp_password_here"
SMTP_FROM="info@kissblow.me"
SMTP_SECURE="false"
# Note: Use port 587 with STARTTLS (SMTP_SECURE=false) or port 465 with SSL/TLS (SMTP_SECURE=true)

# Contact Email (where contact/DMCA form submissions will be sent)
CONTACT_EMAIL="info@kissblow.me"

# Cron Job Secret (for securing the subscription check endpoint)
# Generate a random string and use it in the Authorization header: Bearer <CRON_SECRET>
# Leave empty to disable authentication (not recommended for production)
CRON_SECRET="your_random_secret_string_here"

# CSP Report URI (optional, for monitoring CSP violations)
# Set to your CSP reporting endpoint URL to receive violation reports
# Example: https://csp-report.example.com/report
# Leave empty to disable CSP reporting
CSP_REPORT_URI=""

